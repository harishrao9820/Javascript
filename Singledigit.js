/*
JS Program to check wheather number is single digit are not
*/

let num=-9;

if(num>=-9 && num<=9)
{
    console.log(`${num} is a single digit`);
}
else{
    console.log(`${num} is not a single digit`);
}