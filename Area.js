/*
JS Program to find Area of Circle
*/

let r=2.3, area=0;
//define constant
const  pi=3.14;

area=pi*r*r;

console.log("The Area of Circle with radius "+r+" is : "+area.toFixed(2));
console.log(`Display string and variables using template literals`);
console.log(`The Area of Circle with radius ${r} is : ${area.toFixed(2)}`);
