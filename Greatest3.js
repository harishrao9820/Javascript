/*
JS Program to find greatest of three numbers
*/

let a=300,b=600,c=9;

if (a>b && a>c)
    {
        console.log(`${a} is greatest`);
}
else if(b>c){
    console.log(`${b} is greatest`);
}
else {
    console.log(`${c} is greatest`);
}